# 작업 로그 - 2026-06-26

## 대상
- 국내 생산·포장 현황 대시보드
- 주요 파일: `dashboard.py`, `exports/dashboard_static.html`
- 주요 데이터:
  - `생산요청등록(국내).xlsx`
  - `포장설비투입현황.xlsx`
  - `수요정보(전공정).xlsx`
  - `용마WMS재고현황.xlsx`
  - `클라렌사업본부 재고현황_260626.xlsx`

## 오늘 확정한 방향
- 대시보드의 목적은 국내 요청 물량이 `생산 -> 포장 -> 용마 입고`까지 정상적으로 진행되는지 확인하고, 부족 및 지연 품목을 우선 대응하는 운영 모니터링으로 정리했다.
- 메인 화면은 전체 리스크를 빠르게 판단하는 화면으로 유지하고, 상세 내역은 팝업으로 분리하는 방식을 표준으로 삼았다.
- PACK/PCS 단위 혼동을 줄이기 위해 KPI명과 컬럼명에 단위를 명확히 표시하는 방향을 유지한다.
- 본품과 샘플은 진행률과 부족 리스크가 다르므로 KPI를 분리해서 볼 수 있도록 구성했다.

## 제품 진도 현황
- 상단 KPI를 국내 요청량, 생산진도율, 포장진도율, 용마입고율, 미입고 PACK, 긴급 대응 품목 수 중심으로 재배치했다.
- 기존 `우선 대응` 표현은 제거하고 `용마입고율`로 통일했다.
- 요청 대비 용마 입고 현황 카드에 요청 PACK, 용마입고 PACK, 미입고 PACK, 용마입고율을 함께 표시하도록 정리했다.
- 본품 분류별 진도현황 카드는 제품분류명, 요청 PACK, 생산진도율, 용마입고율, 생산부족 PCS만 남기고 단순화했다.
- 미입고 TOP10에서는 추정 원인 컬럼을 제거하고, 미입고 PACK과 생산/포장/입고 진도율 중심으로 판단하도록 정리했다.
- 생산/입고 GAP 영역은 `생산완료 후 미입고 TOP10`으로 명칭을 바꿨다.

## 생산코드 상세
- 메인 테이블은 생산코드 앞 5자리 기준으로 집계하도록 변경했다.
- 메인 테이블은 P로 시작하는 생산코드만 표시하도록 정리했다.
- PACK 구성은 1P, 2P, 5P, 6P, 10P, 30P, 40P, 80P, 90P 단위별 컬럼으로 표시한다.
- 상태 컬럼은 메인 테이블과 팝업 테이블 모두에서 제거했다.
- 행 선택 시 팝업을 띄워 선택한 생산코드의 POWER별 상세를 확인하도록 구성했다.

## 일일 재고 대응
- 일일 재고 대응 테이블도 팝업 조회 방식으로 변경했다.
- 메인 테이블은 품목코드 `S+숫자3자리` 기준으로 집계해서 보여준다.
- 메인 테이블에 `용마입고 PACK`을 추가했다.
- 메인 테이블과 팝업 테이블에서 `미입고 PACK` 컬럼을 제거했다.
- `용마입고 PACK`과 `용마입고대기 PACK`의 의미를 아래 기준으로 분리했다.
  - `용마입고 PACK`: 실제 용마입고수량
  - `용마입고대기 PACK`: 포장실적과 용마입고수량을 LOTNO 기준으로 매칭한 뒤, 아직 용마입고되지 않은 포장실적 수량
- `용마입고대기 PACK`은 LOTNO 매칭 결과의 입고대기수량만 사용하도록 고정했다.
- 기존 보정식인 `포장 PACK - 용마입고 PACK`은 일일 재고 대응 화면의 `용마입고대기 PACK` 산식에서 제외했다.

## 데이터 반영 기준
- `포장설비투입현황.xlsx`의 포장실적과 용마입고수량이 대시보드에 반영되도록 확인했다.
- `클라렌사업본부 재고현황_260626.xlsx`를 일일 재고 대응 표준 입력 파일로 반영했다.
- 표준 파일명 패턴은 `클라렌사업본부 재고현황_YYMMDD.xlsx`로 본다.
- 사용자가 매일 같은 형식의 재고현황 파일을 넣으면, 대시보드는 최신 파일을 자동으로 찾아 일일 재고 대응, 긴급 요약, 보고서 예외 대응에 사용한다.
- 코드상 표준 키워드는 `클라렌사업본부 재고현황`, `재고현황_`로 분리해 관리한다.

## 오후 5:08 기준 추가 반영
- 사용자가 앞으로 `C:\Users\유현아\Documents\GitHub\Interojo-domestic\클라렌사업본부 재고현황_260626.xlsx` 형식의 파일을 계속 넣겠다고 확정했다.
- 해당 파일 계열을 일일 재고 대응의 표준 데이터 소스로 기록했다.
- 신규 일자 파일도 같은 패턴이면 별도 코드 변경 없이 최신 파일을 기준으로 반영하는 방향으로 정리했다.

## PPT 보고서
- PPT 보고서 다운로드 화면은 운영 보고서형 디자인으로 정리했다.
- 흰색 배경, 얇은 회색 테두리, 제한된 강조색을 기준으로 복잡한 카드/그림자 느낌을 줄였다.
- 전체 KPI, 본품 KPI, 샘플 KPI를 보고서형 카드로 정리했다.
- 하단은 우선 대응 TOP 목록과 일일 재고표 기준 예외 대응을 한 장 안에서 확인하도록 구성했다.

## 검증
- `python -m py_compile dashboard.py export_dashboard_html.py` 통과.
- 실제 데이터 로드 기준으로 포장실적, 용마입고수량, LOT 입고대기수량 반영을 확인했다.
- 일일 재고 대응 메인 테이블에서 `요청 PACK`, `용마입고 PACK`, `용마입고대기 PACK` 표시를 확인했다.
- 일일 재고 대응 메인/팝업 테이블에서 `미입고 PACK` 컬럼이 제거된 것을 확인했다.
- S129 기준으로 메인 합계와 포장 LOT 상세 합계가 일치하는 것을 확인했다.
- 브라우저에서 대시보드 로드, 일일 재고 대응 탭 전환, 팝업 열기/닫기를 확인했고 콘솔 오류는 없었다.
