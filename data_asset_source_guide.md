# 국내 출고 제품 생산·포장 현황 데이터 구성 설명서

## 목적
국내 출고 제품 생산·포장 현황 대시보드는 ERP, APS, WMS/메일 기반 재고 자료, 코드 매칭 마스터를 결합해 판매코드와 생산코드 기준의 생산·포장·입고 진행 현황을 확인하기 위한 운영 데이터 자산이다.

이 문서는 타부서 공유를 위해 각 데이터가 어디에서 오고, 어떤 기준으로 대시보드에 반영되는지 설명한다.

## 전체 데이터 구성

| 필요데이터 | 원천데이터 | 대시보드 내 역할 | 반영 기준 | 비고 |
|---|---|---|---|---|
| 생산요청등록(국내) | ERP | 생산 요청/지시 기준 물량 | `생산지시물량` 시트를 진도율 기준 물량으로 사용 | `3Q전체물량`은 비교·참고용 |
| 수요정보(전공정) | APS - 관변 공장별 이니셜별 45일 | 생산부족수량, 생산계획일, 생산완료예상일 | 생산코드(P코드), POWER 기준으로 매칭 | APS 아웃바운드 |
| 클라렌사업본부 재고현황 | 메일 | 일일 재고 대응, 용마 보유재고, 긴급/부족 판단 | 판매코드(S코드) 기준 집계 | 최신 메일 첨부 기준 |
| 판매코드-제품코드 매칭 마스터 데이터 | 수기/관리 마스터 | 판매코드(S)와 생산코드(P) 연결, 표준 제품명 보강 | 시트2의 제품명을 판매/생산코드 화면의 표준 제품명으로 우선 사용 | 코드 매칭 기준 마스터 |
| 포장실적 | ERP - 포장설비투입현황 | 포장진도율, 포장부족수량 | 판매코드 + POWER + PACK 기준으로 요청 대비 포장실적 반영 | 요청 초과 실적은 진도율에 초과 가산하지 않음 |
| 재고이동현황 | ERP - 재고이동현황 | 용마입고수량, 샘플입고수량, 용마입고율 | `상태=확인/완료`, `입고창고=용마창고/샘플창고`, `이동수량` 기준 | 기존 `용마이동현황`, `샘플이동` 대체 |
| 샘플신청가능수량 | APS | 샘플 포장가능재고 | J열 기준 수량 반영 | APS 아웃바운드 신청중 |
| ODV_WIP | 공정 WIP | 검사접착, 누수규격검사 공정 수량 | 생산코드(P) + POWER 기준 | 생산 현황 팝업 상세에 표시 |

## 코드 기준

| 코드 유형 | 의미 | 사용 위치 |
|---|---|---|
| S로 시작하는 코드 | 판매코드 | 포장 현황, 일일 재고 대응, 제품별 현황 |
| P로 시작하는 코드 | 생산코드 | 생산 현황, 생산부족수량, 생산완료예상일 |

판매코드와 생산코드는 동일한 코드로 보지 않는다. 두 코드의 연결은 `판매코드-제품코드 매칭 마스터 데이터.xlsx`와 APS 수요정보를 함께 사용한다.

## 주요 계산 기준

### 1. 요청수량
- 기본 기준은 `생산요청등록(국내).xlsx`의 `생산지시물량`이다.
- KPI와 주요 테이블은 PCS를 주 단위로 표시하고, PACK은 보조 단위로 표시한다.
- `3Q전체물량`은 운영 진도율의 분모로 사용하지 않는다.

### 2. 생산진도율
- 기준: 생산지시 PCS 대비 생산부족수량 차감 기준.
- 생산부족수량은 `수요정보(전공정)`의 생산코드(P코드) 및 POWER 기준으로 가져온다.
- 생산부족수량이 확인되지 않거나 0이면 생산완료로 본다.

### 3. 포장진도율
- 기준: 요청수량 대비 포장실적.
- 판매코드(S코드), PACK, POWER를 가능한 상세하게 매칭한다.
- 상세코드별 요청량을 초과한 포장실적은 초과분을 추가 가산하지 않는다.

### 4. 용마입고율
- 기준: 요청수량 대비 재고이동 입고수량.
- `재고이동현황`에서 `입고창고=용마창고` 또는 `샘플창고`인 확인 완료 이동수량을 사용한다.
- 본품과 샘플 모두 같은 `재고이동현황` 기준으로 반영한다.

### 5. 생산완료예상일
- 기준: `수요정보(전공정)`의 `[80]누수/규격검사` 계획일 + 5일.
- 생산완료 상태인 경우 생산완료예상일은 `-`로 표시한다.
- 계획일이 없는 경우도 `-`로 표시한다.
- 같은 P코드 제품은 동일한 생산완료예상일 기준으로 표시한다.

## 화면별 데이터 사용 기준

| 화면 | 주요 기준 | 설명 |
|---|---|---|
| 생산 포장 종합 현황 | 생산지시물량 + 포장실적 + 재고이동현황 | 전체/본품/샘플 KPI와 제품 분류별 진도 확인 |
| 일일 재고 대응 현황 | 클라렌사업본부 재고현황 + 생산/포장/입고 데이터 | 판매코드 기준 긴급/부족 대응 품목 확인 |
| 포장 현황 | 판매코드 기준 포장부족 PCS | 용마 재고 수준과 포장부족을 기준으로 긴급 포장 리스트 확인 |
| 생산 현황 | 생산코드 기준 생산부족 PCS | 생산코드별 생산 진행률, 포장 진행률, 생산완료예상일 확인 |
| 제품별 현황 | 판매코드 기준 PCS 집계 | 영업팀 관점에서 판매코드별 생산완료 및 출고 가능 상태 확인 |

## 데이터 반영 시 주의사항

- 제품명만으로 매칭하지 않는다. 코드, PACK, POWER, CP 값을 우선한다.
- 토릭 제품은 `판매코드 + POWER + CP`를 상세 매칭 키로 사용한다.
- `0.75`, `1.25`, `1.75` 같은 CP 값은 PACK이나 POWER가 아니다.
- `재고이동현황`은 `저장` 상태를 반영하지 않는다.
- 샘플은 별도 창고로 이동될 수 있으므로 `샘플창고` 이동수량도 입고 실적으로 포함한다.
- 표준 제품명은 `판매코드-제품코드 매칭 마스터 데이터.xlsx` 시트2를 우선한다.

## 운영 표준

- 원천 파일명과 시트명은 변경하지 않는 것을 원칙으로 한다.
- 컬럼명이 변경될 경우 대시보드 반영 전에 컬럼 매핑 기준을 먼저 갱신한다.
- 코드 매칭 마스터는 판매코드/생산코드 기준 제품명을 관리하는 기준 자료로 유지한다.
- 데이터 변경 후에는 전체 KPI, 본품 KPI, 샘플 KPI의 요청수량, 포장진도율, 용마입고율을 함께 검증한다.
