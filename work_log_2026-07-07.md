# 작업 로그 - 2026-07-07

## 운영 기준 정리
- `S`로 시작하는 코드는 `판매코드`, `P`로 시작하는 코드는 `생산코드`로 통일했다.
- 사용자 화면에서 S계열 코드를 의미하는 `품목코드` 표현은 `판매코드`로 바꾸는 것을 표준으로 정했다.
- 각 화면의 주요 필터와 테이블에서는 `공장구분` 대신 `기간구분`을 사용한다. `공장구분`은 원천 데이터 보강 또는 내부 매칭용으로만 남긴다.
- 대표 제품명은 가능한 한 마스터/표준 제품명을 우선 표시한다.

## KPI 계산 기준
- 전체 KPI, 본품 KPI, 샘플 KPI가 서로 다른 집계 경로를 타지 않도록 생산코드 요약 기준으로 통일했다.
- `포장진도율 = 인정 포장 PACK / 요청 PACK`으로 계산한다.
- `용마입고율 = 용마입고 PACK / 요청 PACK`으로 계산한다.
- 포장진도율과 용마입고율은 같은 요청 PACK을 분모로 쓰지만 분자가 다르므로, 서로 같게 보정하지 않는다.
- 화면 표시 직전에 다른 집계로 KPI 값을 덮어쓰는 방식은 사용하지 않는다.
- 샘플은 일반 용마 창고가 아니라 별도 샘플 창고로 이동되므로, `포장설비투입현황.xlsx`의 `샘플이동` 시트를 샘플 입고 실적으로 반영한다.
- `샘플이동` 시트는 `상태=확인/완료`, `입고창고=샘플창고` 기준으로 읽고, `품목코드`와 `이동수량`을 판매코드 단위 입고 수량으로 본다.

## 코드 매칭 기준
- 생산/포장/입고 진도율은 제품명만으로 매칭하지 않고, 가능한 경우 판매코드, 생산코드, PACK, POWER, CP를 함께 사용한다.
- 토릭 제품은 `판매코드 + POWER + CP`를 상세 키로 본다. 예: `S524-08.00-1.75`.
- 토릭의 `0.75`, `1.25`, `1.75`는 CP 표기이므로 PACK 또는 POWER로 오인하지 않는다.
- 생산부족수량과 생산진도율은 포장실적으로 보정하지 않고 전공정 원천값을 그대로 표시한다.
- 전공정 원천 생산부족 기준과 포장실적 반영 기준이 달라지는 행은 `기준차이` 배지만 붙이고 수치는 그대로 보여준다.

## 화면 구성 기준
- 메인 대시보드는 `Header -> 기간구분 -> 전체 KPI -> 본품/샘플 KPI -> 제품 분류별 진도현황` 흐름을 기준으로 정리했다.
- 메인 대시보드에서 `미입고 TOP10`, `요청 긴급 대응`, `제품 상세 테이블`은 제거하는 기준으로 정했다.
- 판매코드 상세는 `포장 우선순위`와 `판매코드 세부리스트` 내부 탭으로 분리해 스크롤을 줄였다.
- `POWER 상세`는 상단 메뉴에서 제거하고, 필요한 상세는 생산코드/판매코드 화면의 팝업 또는 세부 리스트에서 확인한다.
- 사이드바 하단의 `분석·리포트`, `긴급 대응 목록`, `이슈 & 알림`, `보고서 관리`, `필터` 안내 영역은 제거했다.

## 디자인 시스템 기준
- 전체 디자인은 생산기획팀이 장시간 보는 운영형 Enterprise Dashboard 기준으로 정리했다.
- 색상은 Blue, Orange, Purple, Red, Gray, White 중심으로 제한하고 Green은 주요 진행률 색상으로 사용하지 않는다.
- 전체 KPI만 컬러를 사용해 강조하고, 본품 KPI와 샘플 KPI는 회색 톤을 기본으로 한다.
- Progress Bar는 4px 두께, radius 999px를 기본으로 유지한다.
- 테이블은 White 배경, `#E5E7EB` Border, `#F8FAFC` Hover로 통일하고 Zebra Striping, 누런 배경, Legacy Red Theme를 제거한다.
- 생산진도는 Blue, 포장진도는 Orange, 용마입고율은 Purple, 생산부족은 Red Text로 표시한다.

## PPT 보고서 기준
- PPT 보고서 본문에는 긴급 판매코드 요약 대신 `제품 분류별 진도현황`을 우선 표시하는 기준으로 정했다.
- 보고서도 대시보드와 같은 생산/포장/용마입고 색상 체계를 따른다.

## 검증 기준
- `dashboard.py` 수정 후 문법 검사를 실행한다.
- KPI 확인 시 요청 PACK, 인정 포장 PACK, 용마입고 PACK을 함께 확인해 포장진도율과 용마입고율이 같은 기준에서 계산되는지 본다.
- 샘플 이동 반영 후 현재 파일 기준 샘플 원천 이동수량은 11,801, 인정 샘플 입고 수량은 8,814 PACK, 샘플 용마입고율은 28.9%로 확인했다.
- 화면 확인은 기본적으로 배포된 Streamlit 화면 또는 사용자가 제공한 화면 기준으로 한다. 로컬 서버는 사용자가 명시적으로 요청한 경우에만 띄운다.
