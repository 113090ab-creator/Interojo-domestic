# 작업 로그 - 2026-07-08

## 용마입고율 재점검
- 원천 데이터 기준 `포장설비투입현황.xlsx`의 용마입고 수량은 6,908건 / 143,279 PACK으로 확인했다.
- 생산지시 기준으로 코드별 요청 수량을 초과하지 않도록 인정 처리한 용마입고 수량은 109,429 PACK으로 확인했다.
- 현재 KPI 재계산값은 전체 포장 43.8% / 용마 40.2%, 본품 포장 45.6% / 용마 41.6%, 샘플 포장 28.9% / 용마 28.9%다.

## 캐시 재발 방지
- 데이터 캐시 키를 `DATA_CACHE_VERSION + dashboard.py 경로 + dashboard.py 내용 해시` 기준으로 강화했다.
- 코드 파일의 수정 시간이나 크기만 보는 방식보다 더 확실하게, 계산/매칭 로직이 조금이라도 바뀌면 이전 KPI 캐시를 재사용하지 않도록 했다.
- `DATA_CACHE_VERSION`을 36으로 올려 기존 배포 캐시도 강제로 무효화되도록 했다.

## 제품 분류별 진도현황 표시 기준
- 요청 PACK 또는 요청 PCS가 0인 제품 분류는 제품 분류별 진도현황에서 제외하도록 수정했다.
- `PIA 1Day`는 현재 요청 물량이 없어 표시 대상에서 제외되는 것을 확인했다.
- 현재 파일 기준 제품 분류별 진도현황은 요청 물량이 있는 14개 분류만 표시된다.

## 제품별 생산 완료 현황 추가
- 제품 진도 현황 화면의 제품 분류별 진도현황 아래에 `제품별 생산 완료 현황` 영역을 추가했다.
- 메인 테이블은 `S###` 판매코드 기준 1행 집계로 표시하고, 행 선택 시 POWER 단위 상세 팝업을 표시하도록 했다.
- 기간구분, 제품분류, 판매코드 검색, 제품명 검색, 생산상태 필터를 추가했다.
- 기간구분은 생산완료예상일 기준으로 `전체`, `오늘`, `이번주`, `다음주`, `사용자 지정 기간`을 지원한다.
- 생산완료예상일은 `수요정보(전공정).xlsx`의 `[80]누수/규격검사` 생산 계획일 + 5일 기준으로 계산한다.
- 수요정보 날짜 헤더가 `납기일`이 아니라 `생산 계획일`로 들어오는 파일도 인식하도록 계획일 컬럼 탐색 규칙을 보완했다.
- 실제 파일 기준 새 집계는 판매코드 69건, POWER 상세 1,129건으로 생성되는 것을 확인했다.
- 상태 분포는 생산중 42건, 미계획 25건, 생산완료 2건으로 확인했다.

## 수요정보 계획일 기준 보정
- 수요정보 기반 `production_due_date` 공용 컬럼도 `[80]누수/규격검사` 생산 계획일 + 5일 기준으로 통일했다.
- 수요정보 월 필터도 기존 총합계 납기/계획일이 아니라 `[80]누수/규격검사` 생산 계획일 + 5일 기준으로 적용하도록 변경했다.
- 생산요청 데이터의 날짜 컬럼 후보에 `계획일자`, `계획일`, `생산계획일자`, `생산 계획일`을 추가했다.
- 실제 파일 기준 수요정보 1,251건 중 완료예상일이 대상 월에 들어오는 437건이 생산진도 기준으로 필터링되는 것을 확인했다.

## KPI 수량 단위 표시
- 전체/본품/샘플 KPI 카드의 `요청 PACK`, `미입고 PACK` 표기를 `요청수량`, `미입고수량`으로 변경했다.
- KPI 수량은 PCS를 큰 주값으로 표시하고 PACK은 회색 보조값으로 괄호 표기하도록 통일했다.
- 실제 파일 기준 전체 KPI는 요청 9,348,865 PCS (272,495 PACK), 미입고 4,650,168 PCS (152,109 PACK)로 생성되는 것을 확인했다.

## 화면 메뉴 및 명칭 정리
- 대시보드 제목은 `국내 출고 제품 생산·포장 현황`으로 정리했다.
- 사이드바 메뉴명은 다음처럼 운영 업무 기준으로 변경했다.
  - `제품 진도 현황` -> `생산 포장 종합 현황`
  - `일일 재고 대응` -> `일일 재고 대응 현황`
  - `판매코드 상세` -> `포장 현황`
  - `생산코드 상세` -> `생산 현황`
- 메뉴 순서는 `생산 포장 종합 현황 -> 일일 재고 대응 현황 -> 포장 현황 -> 생산 현황`으로 정리했다.
- `POWER 상세`, `포장 LOT 상세`는 상단 메뉴에서 제거하고 필요한 상세는 팝업과 세부 리스트에서 확인하는 방식으로 정리했다.

## 테이블 구조 표준화
- 일일 재고 대응, 포장 현황, 생산 현황의 주요 테이블은 2단 헤더를 사용하는 방향으로 정리했다.
- 일일 재고 대응 테이블은 `대응정보 / 제품정보 / 요청·재고현황 / 입고·포장현황 / 생산정보` 그룹으로 구성했다.
- 포장 현황 테이블은 `제품정보 / 요청·입고현황 / 포장·생산현황 / 완료정보` 그룹으로 구성했다.
- 생산 현황 테이블은 `제품정보 / 요청·포장현황 / 생산현황 / 완료정보` 그룹으로 구성했다.
- 진행률 컬럼은 숫자만 표시하지 않고 기존 표준 막대바 형식으로 표시하도록 유지했다.
- 테이블 제목/설명이 화면 흐름상 중복되는 경우 제거했다. 포장 현황의 판매코드 세부리스트 상단 `판매코드` 제목과 표시 건수 설명은 제거했다.
- 생산 현황의 `생산코드 메인 테이블` 문구는 `생산코드 세부 현황`으로 변경했다.

## 포장 현황 기준 정리
- 포장 현황 화면은 PCS 기준으로 통일했다. `PACK 기준 / PCS 기준` 조회 단위 선택은 제거했다.
- `PACK 선택`은 표시 단위가 아니라 제품 포장 단위 필터로 유지했다.
- 긴급 포장 리스트와 판매코드 세부리스트의 요청/입고/대기/부족 수량은 PCS 기준으로 표시하도록 변경했다.
- 포장 현황의 `우선등급`은 내부 정렬과 긴급 리스트 선별에는 사용하지만 화면 컬럼으로는 표시하지 않도록 정리했다.
- 사용자가 보는 판단 컬럼은 `완료정보 > 상태`로 통일했다.
- 우선등급 내부 계산 기준은 다음으로 확인했다.
  - 포장부족 0 이하: `완료`
  - 생산완료예상일이 오늘 이전/당일: `A 긴급`
  - 생산완료예상일 3일 이내 + 용마 보유재고가 긴급 기준 이하: `A 긴급`
  - 생산완료예상일 7일 이내 또는 용마 보유재고가 긴급 기준 이하: `B 주의`
  - 그 외 부족 건: `C 일반`

## 생산 현황 기준 정리
- 생산 현황 화면의 분류 선택 필터는 제거하고 `통합검색 / PACK 선택 / POWER 선택 / 부족품만 보기` 기준으로 정리했다.
- 생산코드 메인 테이블에서는 PACK별 요청 수량 컬럼을 제거하고, 요청/포장부족/포장가능재고/생산부족을 PCS 중심으로 표시하도록 정리했다.
- 생산코드 메인 테이블과 팝업의 숫자 컬럼은 천단위 쉼표를 적용했다.
- 생산완료예상일은 같은 P코드 제품이면 가장 늦은 예상일을 동일하게 보여주는 방향으로 정리했다.
- 생산완료 상태이거나 생산부족수량이 없는 경우 생산완료예상일은 `-`로 표시하도록 정리했다.
- 생산코드 팝업에는 `ODV_WIP.xlsx`를 활용해 `검사접착`, `누수규격검사` 공정 수량을 표시하도록 추가했다.
- 생산코드 팝업의 PACK 구성 현황에서 구성비 컬럼은 제거했다.

## 제품명 및 코드 표시 기준
- 판매코드와 생산코드 기준 화면에서 사용할 제품명은 `판매코드-제품코드 매칭 마스터 데이터.xlsx`의 시트2 제품명을 우선 활용하도록 정리했다.
- 같은 제품군이라도 P코드가 다르면 다른 제품으로 표시해야 하므로 P코드 기준 제품명을 유지한다.
- 예시로 `P4843`은 `Toric_CP-0.75_Halo Brown`, `P4845`는 `Toric_CP-1.75_Halo Brown`처럼 구분한다.

## 검증 및 운영 기준
- 코드 수정 후 `python -B -m py_compile dashboard.py` 검증을 수행했다.
- 사용자가 명시하지 않는 한 로컬 서버를 별도로 띄우지 않고, 배포 화면 또는 사용자가 제공한 캡처 기준으로 확인하는 운영 기준을 유지했다.
