# Work Log - 2026-05-27

## 1) Data/Input Handling
- Replaced upload-driven flow with automatic file loading from `data/`.
- Added auto-discovery for:
  - request source: `생산요청등록(국내)_5월.xlsx`
  - packing source: `포장설비투입현황.xlsx`
  - mapping source: `판매코드-제품코드 매칭 마스터 데이터.xlsx`
- Added file existence checks and user-facing error messages when files/required columns are missing.

## 2) Dashboard Logic (`dashboard.py`)
- Rebuilt `dashboard.py` into a single integrated app.
- Implemented column alias matching to reduce column-name mismatch failures.
- Implemented mapping-based linking (sales code / production code / product code).
- Calculated:
  - production request qty
  - packing completed qty
  - remain qty
  - progress rate
- Implemented status classification:
  - `미착수` / `진행중` / `완료` / `초과포장`

## 3) UI/Structure Updates
- Added filter/search area (product code/name, sales code, power, status).
- KPI cards now recalculate based on filtered results.
- Added tab layout:
  - `전체`
  - `제품코드 기준 요약`
  - `상세`
  - `미착수`
  - `진행중`
  - `완료`
  - `초과포장`
- Added `잔여수량 TOP 20` area.
- Added Excel download button for filtered results.

## 4) Quantity/Progress Fixes
- Fixed fractional quantity issue:
  - changed allocation logic to integer allocation for PACK-based fields.
- Progress display changed to percentage text format:
  - e.g., `87.3%` instead of `87.3`.

## 5) Runtime/Operational Stabilization
- Updated `run_streamlit.bat`:
  - fixed to port `8514`
  - log output to:
    - `streamlit_8514_out.log`
    - `streamlit_8514_err.log`
  - added auto-restart loop behavior.
- Updated `open_dashboard.bat` startup flow and readiness checks.
- Added `restart_dashboard_8514.bat` for one-shot recovery:
  - stop port listener
  - restart dashboard
  - reopen browser

## 6) Key Files Touched
- `dashboard.py`
- `run_streamlit.bat`
- `open_dashboard.bat`
- `start_dashboard.ps1` (intermediate update)
- `restart_dashboard_8514.bat` (new)
- `work_log_2026-05-27.md` (this log)

## 7) Current Run Command
```bash
python -m streamlit run dashboard.py --server.port 8514
```

## 8) Operational Note
- If browser shows `ERR_CONNECTION_REFUSED`, verify server is alive first.
- Quick recovery:
  - run `restart_dashboard_8514.bat`
  - then refresh `http://127.0.0.1:8514`.

## 9) Additional Updates (Evening)
- Unified search UI:
  - replaced separate inputs (`제품코드/제품명/판매코드/파워`) with one integrated search box.
  - search now applies partial match, case-insensitive, across all four columns.
  - status filter remains separate.
- Added power-based heatmap dashboard section:
  - product selector (`제품명 또는 제품코드 선택`)
  - tabs:
    - `제품별 도수별 포장진도율`
    - `제품별 도수별 잔여수량`
    - `도수별 상세 리스트`
  - all heatmaps/tables refresh from filtered dataset.
- Fixed runtime error:
  - `AttributeError: 'DataFrame' object has no attribute 'applymap'`
  - updated heatmap text formatting to use `DataFrame.map` with fallback compatibility.
- Standardized power sorting:
  - separated internal numeric column and display column.
  - added safe numeric parsing for mixed strings/spaces.
  - kept display format as `-00.00`.
  - applied same numeric ordering to heatmap X-axis and power detail list.
