# 작업 로그 - 2026-07-03

## 대상
- 국내 생산·포장 현황 대시보드
- 주요 파일: `dashboard.py`
- 주요 데이터:
  - `생산요청등록(국내).xlsx`
  - `포장설비투입현황.xlsx`
  - `수요정보(전공정).xlsx`
  - `판매코드-제품코드 매칭 마스터 데이터.xlsx`
  - `용마WMS재고현황.xlsx`
  - `클라렌사업본부 재고현황_260703.xlsx`

## 오늘 반영한 내용
- `생산요청등록(국내).xlsx`가 `생산지시물량`, `3Q전체물량` 시트로 분리된 구조에 맞춰 진도 기준을 `생산지시물량` 중심으로 정리했다.
- `포장설비투입현황.xlsx`의 샘플신청가능수량 반영 열을 J열 기준으로 변경했다.
- 신규분류요약별 요청 대비 지시 수준 화면은 접기/펼치기 가능한 영역으로 바꾸고, 기본 상태를 접힘으로 설정했다.
- 지시율은 숫자만 보이던 값을 `%` 단위로 표시하도록 정리했다.
- 일일 재고 대응 테이블에서 `(미기재)`가 노출되지 않도록 보정하고, 공장구분과 대표 제품명은 표준 제품명 기준으로 보강했다.
- 판매코드 상세 화면에서 `D-Day` 컬럼을 제거하고, 그 위치에 `공장구분`을 배치했다.
- 메인 메뉴의 주요 TOP10/요약 표 너비가 서로 어긋나지 않도록 정리했다.
- 판매코드 상세 메인 테이블을 `S+숫자3자리` 기준으로 집계하도록 변경했다.
- 판매코드별 POWER 포함 상세 내역은 메인 테이블에 펼쳐 놓지 않고 팝업에서 확인하도록 변경했다.
- 판매코드 상세 메인 테이블의 `상세 건수`와 `POWER 수`가 동일하게 보이는 중복성을 줄이기 위해 `상세 건수` 컬럼을 제거했다.
- 판매코드 상세 조건 영역은 공장구분, 조회 단위, 긴급 재고 기준을 가로 배치로 정리했다.
- 판매코드 상세 검색은 제품명/판매코드/생산코드 개별 검색에서 `통합검색` 하나로 변경했다.
- 통합검색은 메인 테이블의 주요 컬럼 전체를 대상으로 검색하고, 쉼표로 구분한 다중 검색을 지원하도록 했다.
- 생산코드 상세 검색도 `통합검색 + PACK 선택 + POWER 선택` 구조로 변경했다.
- 생산코드 상세 통합검색은 제품명, 생산코드, 판매코드, 공장구분, PACK, POWER를 함께 조회하도록 했다.
- 제품명 검색은 공백, 언더바, 하이픈, 특수문자, 대소문자 차이를 무시하도록 보강했다.
- `SOUL BROWN`, `SOULBROWN`, `소울브라운`, `*IRIS SOULBROWN`처럼 같은 제품을 다르게 입력해도 검색되도록 별칭 기준을 반영했다.
- 다른 주요 제품도 한글 별칭으로 검색할 수 있도록 `재즈블랙`, `수지브라운`, `수지그레이`, `랩소디`, `라틴`, `뮤트브라운`, `딥블랙` 등 별칭을 추가했다.
- 일일 재고 대응 메인 테이블에서 `상세 건수`와 `긴급요청 수`가 동일하게 보이는 중복성을 줄이기 위해 `상세 건수`를 제거했다.
- 일일 재고 대응 메인 테이블에서 `재고부족수량` 컬럼을 제거했다.

## 매칭/진도 검토 내용
- 미입고 TOP10은 미입고 PACK이 큰 판매 PACK 제품 기준으로 보는 것이 맞다는 점을 확인했다.
- 생산진도율과 포장진도율은 제품명만으로 맞추면 왜곡될 수 있어, 판매코드 S계열과 생산코드 P계열의 매칭 규칙을 중요 기준으로 삼기로 했다.
- `판매코드-제품코드 매칭 마스터 데이터.xlsx`와 `수요정보(전공정).xlsx`를 함께 활용해 S코드와 P코드를 연결하는 방향으로 정리했다.
- POWER를 포함한 상세 코드 기준으로 요청수량과 실적수량을 비교해야 한다는 기준을 확인했다.
- 상세코드별 요청 초과 포장분을 별도 가산해서 진도율을 높이는 방식은 사용하지 않는 것으로 정리했다.
- 포장진도율은 같은 상세 기준에서 `실제 포장 PACK / 요청 PACK`으로 해석해야 한다.
- 전체/본품/샘플 KPI의 포장진도율 기준을 통일했다. 계산은 코드별 요청수량을 초과한 포장분을 제외한 `인정 포장 PACK / 요청 PACK`으로 한다.
- 현재 데이터 기준 포장진도율은 전체 39.2%, 본품 40.5%, 샘플 28.9%로 확인했다.
- P0019, Iris JazzBlack 관련 수치는 수요정보 업데이트 후 다시 확인하기로 했다.

## 검증
- `python -m py_compile dashboard.py` 통과.
- 실제 데이터 기준 검색 검증:
  - `SOUL BROWN` -> `S129` 포함 확인
  - `SOULBROWN` -> `S129` 포함 확인
  - `소울브라운` -> `S129` 포함 확인
  - `재즈블랙` -> `S130` 포함 확인
  - `S145, S155` -> 두 판매코드 모두 조회 확인
- 포장진도율 KPI 검증:
  - 전체: 요청 272,495 PACK / 인정 포장 106,802 PACK -> 39.2%
  - 본품: 요청 241,980 PACK / 인정 포장 97,988 PACK -> 40.5%
  - 샘플: 요청 30,515 PACK / 인정 포장 8,814 PACK -> 28.9%
- 대시보드 서버 재시작 완료: `http://127.0.0.1:8514`

## 남은 확인
- P0019 및 Iris JazzBlack의 생산/포장 진도율은 수요정보가 업데이트된 뒤 다시 검토한다.
- S코드와 P코드 매칭 품질은 마스터 데이터와 수요정보가 바뀔 때마다 샘플 제품으로 재검증한다.
