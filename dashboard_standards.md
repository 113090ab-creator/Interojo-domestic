# 대시보드 운영 표준

## 데이터 기준
- 진도체크의 기준 물량은 `생산요청등록(국내).xlsx`의 `생산지시물량`을 우선 사용한다.
- `3Q전체물량`은 비교 또는 참고용으로만 사용하고, 운영 진도율의 기본 분모로 사용하지 않는다.
- `포장설비투입현황.xlsx`의 샘플신청가능수량은 J열을 기준으로 반영한다.
- `포장설비투입현황.xlsx`의 `샘플이동` 시트는 샘플 제품의 입고 실적으로 반영한다.
- 일일 재고 데이터에서 비어 있는 값은 화면에 `(미기재)`로 노출하지 않는다. 가능한 경우 마스터 데이터, 요청 데이터, WMS 데이터 순으로 보강한다.
- 화면 필터와 주요 테이블에서는 `공장구분` 대신 `기간구분`을 사용한다. `공장구분`은 원천 데이터 보강과 내부 매칭이 필요한 경우에만 사용한다.

## 코드 매칭 기준
- `S`로 시작하는 코드는 항상 `판매코드`로 부른다.
- `P`로 시작하는 코드는 항상 `생산코드`로 부른다.
- 사용자 화면에서 `품목코드`라는 표현이 필요한 경우에도, S계열 코드 의미라면 `판매코드`로 표시한다.
- 판매코드는 메인 집계에서 `S+숫자3자리` 기준으로 묶는다.
- 생산코드는 P계열 생산코드를 기준으로 보고, 판매코드 S계열과 직접 동일 코드처럼 취급하지 않는다.
- S코드와 P코드의 연결은 `판매코드-제품코드 매칭 마스터 데이터.xlsx`와 `수요정보(전공정).xlsx`를 함께 활용한다.
- 생산/포장/입고 진도율은 제품명만으로 매칭하지 않는다. 가능한 경우 품목코드, 판매코드, 생산코드, PACK, POWER를 포함한 상세 키로 비교한다.
- POWER가 있는 제품은 POWER를 포함한 상세 코드 기준으로 요청수량과 실적수량을 비교한다.
- 토릭 제품은 `판매코드 + POWER + CP`를 상세 매칭 키로 사용한다. 예: `S524-08.00-1.75`.
- 토릭의 `0.75`, `1.25`, `1.75` 같은 값은 담당자가 붙이는 CP 표기이므로 PACK 또는 POWER로 오인하지 않는다.
- 대표 제품명은 화면 표시 시 가능한 한 마스터/표준 제품명을 우선 사용한다.

## 진도율 계산 기준
- 생산진도율은 생산지시 기준 요청수량 대비 생산 실적 또는 생산부족수량을 기준으로 계산한다.
- 포장진도율은 기본적으로 같은 상세 기준에서 요청 PACK 대비 인정 포장 PACK으로 계산한다.
- 단, 생산코드 관점처럼 30P/90P 등 PACK 단위가 섞여 생산진도율과 직접 비교되는 화면은 요청 PCS 대비 인정 포장 PCS로 계산한다.
- 인정 포장 PACK은 코드별 요청 PACK을 초과하지 않는 범위에서 반영한다.
- 상세코드별 요청 초과 포장분을 별도 가산해서 포장진도율을 높이지 않는다.
- 용마입고율은 요청 PACK 대비 실제 용마입고 PACK으로 계산한다.
- 포장진도율과 용마입고율은 같은 요청 PACK을 분모로 쓰지만, 분자는 각각 인정 포장 PACK과 용마입고 PACK으로 다르다. 두 값을 서로 같게 보정하지 않는다.
- 샘플 제품은 일반 `용마이동현황`에 없을 수 있으므로 `샘플이동` 시트의 `이동수량`을 판매코드 단위 입고 수량으로 함께 반영한다.
- 전체 KPI, 본품 KPI, 샘플 KPI는 같은 생산코드 요약 기준에서 계산한다. 화면 표시 직전에 별도 집계로 덮어쓰지 않는다.
- 미입고 TOP10은 미입고 PACK이 큰 판매 PACK 제품 기준으로 표시한다.

## 화면 구성 기준
- 메인 테이블은 판단에 필요한 요약 단위로 집계하고, POWER 포함 상세는 팝업에서 확인한다.
- 판매코드 상세 메인 테이블은 `S###` 단위 집계를 기본으로 한다.
- 판매코드 상세는 `포장 우선순위`와 `판매코드 세부리스트`를 내부 탭으로 분리해 긴 스크롤을 줄인다.
- `POWER 상세`는 상단 메뉴에서 제거하고, 필요한 상세는 생산코드/판매코드 화면의 팝업 또는 세부 리스트에서 확인한다.
- `포장 LOT 상세`는 상단 메뉴에서 제거한다. LOT 단위 데이터는 별도 화면으로 노출하지 않고, 필요한 경우 내부 매칭 또는 검증용으로만 사용한다.
- 불필요한 중복 컬럼은 메인 테이블에서 제거한다. 예: `상세 건수`와 `POWER 수`가 같은 의미로 보이면 `POWER 수`만 유지한다.
- 일일 재고 대응 메인 테이블에서 `상세 건수`와 `긴급요청 수`가 같은 의미로 보이는 경우 `긴급요청 수`만 유지한다.
- 일일 재고 대응 메인 테이블에는 계산용 보조 컬럼인 `재고부족수량`을 표시하지 않는다.
- 진행률과 지시율은 `%` 단위로 표시한다.
- 상세 설명 또는 보조 요약 영역은 기본 접힘 상태를 우선 적용한다.
- PACK/PCS처럼 단위가 중요한 컬럼은 컬럼명에 단위를 명시한다.
- 메인 대시보드는 `Header -> 기간구분 -> 전체 KPI -> 본품/샘플 KPI -> 제품 분류별 진도현황` 흐름을 기본으로 한다.
- 메인 대시보드에서는 `미입고 TOP10`, `요청 긴급 대응`, `제품 상세 테이블`을 표시하지 않는다.
- PPT 보고서 본문에는 긴급 판매코드 요약 대신 `제품 분류별 진도현황`을 우선 표시한다.
- 제품 분류별 진도현황은 요청 PACK 또는 요청 PCS가 있는 분류만 표시한다. 요청 물량이 0인 분류는 생산부족이나 보조 계산값이 있더라도 표시하지 않는다.

## 디자인 시스템 기준
- 전체 톤은 생산기획팀이 장시간 보는 Enterprise Dashboard 기준으로 유지한다. 화려함보다 정렬, 여백, 숫자 가독성을 우선한다.
- 색상은 `Primary #2563EB`, `Orange #F97316`, `Purple #7C3AED`, `Danger #DC2626`, `Text #111827`, `Sub Text #6B7280`, `Border #E5E7EB`, `Background #F8FAFC`, `Card #FFFFFF`를 기본으로 한다.
- Green은 주요 진행률 색상으로 사용하지 않는다.
- 전체 KPI만 Blue/Orange/Purple/Red로 강조하고, 본품 KPI와 샘플 KPI는 회색 톤을 기본으로 한다.
- KPI 카드의 요청/미입고 수량은 `PCS`를 큰 주값으로 표시하고, `PACK`은 회색 보조값으로 괄호 표기한다. 문구는 `요청수량`, `미입고수량`으로 통일한다.
- KPI 카드의 Progress Bar는 4px, radius 999px를 기본으로 유지한다.
- 테이블은 배경색으로 행을 구분하지 않는다. Header/Cell/Empty 영역은 White, Border는 `#E5E7EB`, Hover는 `#F8FAFC`로 통일한다.
- 테이블 Progress Bar는 생산 Blue, 포장 Orange, 용마입고 Purple을 사용하고, 생산부족은 Red Text만 사용한다.
- Legacy Red Theme, 누런 배경, 회색 빈 영역, Zebra Striping은 사용하지 않는다.

## 검색 기준
- 판매코드 상세 검색은 `통합검색` 하나로 운영한다.
- 생산코드 상세 검색도 `통합검색` 하나로 운영하고, 옆에 `PACK 선택`, `POWER 선택`을 배치한다.
- 통합검색은 메인 테이블의 주요 컬럼 전체를 대상으로 한다.
- 쉼표로 구분한 검색어는 OR 조건으로 처리한다. 예: `S145, S155`.
- 제품명 검색은 공백, 언더바, 하이픈, 특수문자, 대소문자 차이를 무시한다.
- 제품명 한글 별칭은 별칭 표에 등록해 관리한다. 예:
  - `소울브라운` -> `SoulBrown`, `Soul Brown`, `Iris SoulBrown`
  - `재즈블랙` -> `JazzBlack`, `Jazz Black`
  - `수지브라운` -> `Suzy Brown`
  - `수지그레이` -> `Suzy Gray`
- 별칭은 특정 제품 하나에만 예외 처리하지 않고, 같은 규칙으로 다른 제품에도 확장한다.

## 검증 기준
- `dashboard.py`를 수정한 뒤에는 `python -m py_compile dashboard.py`를 실행한다.
- 검색 또는 매칭 로직을 바꾼 경우 실제 데이터 기준 샘플 제품을 확인한다.
- KPI 계산 기준을 바꾼 경우 전체/본품/샘플의 요청 PACK, 포장 PACK, 용마입고 PACK, 포장진도율, 용마입고율이 같은 기준으로 계산되는지 확인한다.
- KPI 또는 매칭 helper 함수가 변경될 수 있으므로 데이터 캐시 키에는 엑셀 파일 지문뿐 아니라 `dashboard.py` 코드 파일 내용 해시를 포함한다. 코드 변경 후 이전 캐시가 KPI를 오염시키는 상황을 허용하지 않는다.
- 조회 성능은 최초 파일 로딩과 이후 필터 조회를 구분해 판단한다. 엑셀 파일을 처음 읽는 시간은 상대적으로 길 수 있으나, 캐시 이후 검색/필터/탭 이동은 빠르게 유지한다.
- 최소 확인 샘플:
  - `SOUL BROWN`, `SOULBROWN`, `소울브라운` -> `S129`
  - `재즈블랙` -> `S130`
  - `S145, S155` -> 두 코드 모두
- 화면 확인은 기본적으로 배포된 Streamlit 화면 또는 사용자가 제공한 화면 기준으로 한다. 로컬 서버는 사용자가 명시적으로 요청한 경우에만 띄운다.

## 생산부족수량 매칭 기준
- 생산진도율은 생산지시물량의 지시 PCS와 `수요정보(전공정).xlsx`에서 확인되는 P코드/POWER 기준 생산부족수량을 비교해 계산한다.
- P코드/POWER 기준 생산부족수량이 확인되지 않는 코드는 생산부족 0으로 본다. 따라서 생산진도율은 100%가 된다.
- 같은 생산코드 또는 P+POWER를 공유하는 판매코드가 여러 개일 때는 제품명이나 PACK명으로 제외하지 않고 모두 연결한다.
- 전공정 원천에 같은 P코드/POWER가 PACK 또는 지시 PACK 수량별로 여러 줄 존재하면, 해당 원천 줄은 PACK 단위와 지시 PACK 수량이 맞는 SKU에 우선 연결한다.
- 전공정 원천 한 줄이 여러 판매코드에 실제로 공유되는 경우에는 판매코드/팩별 상세 화면에 원천 생산부족수량을 비율 배분하지 않고 같은 숫자로 그대로 표시한다.
- 제품 합계, KPI, TOP10처럼 상위 합계를 계산할 때는 같은 원천 P코드/POWER의 생산부족수량을 한 번만 집계해 중복 합산을 막는다.
- 생산부족수량과 생산진도율은 포장실적으로 보정하지 않고 전공정 원천값을 그대로 표시한다.
- 전공정 원천 생산부족 기준과 포장실적 반영 기준이 달라지는 생산코드/P+POWER 행에는 `기준차이` 배지만 표시한다.
- 생산부족 매칭은 제품명보다 판매코드, 생산코드, P코드, POWER 등 코드 규칙을 우선한다.

## 제품별 생산 완료 현황 기준
- 제품별 생산 완료 현황은 제품 진도 현황 화면의 제품 분류별 진도현황 아래에 표시한다.
- 메인 테이블은 `S###` 판매코드 기준으로 1행씩 집계하고, 상세는 판매코드 선택 후 POWER 단위 팝업으로 확인한다.
- 모든 수량은 PCS 기준으로 표시한다.
- 생산완료예상일은 `수요정보(전공정).xlsx`의 `[80]누수/규격검사` `생산 계획일`에 5일을 더해 계산한다.
- 수요정보 날짜 컬럼은 파일 구조 차이를 고려해 `생산 계획일`, `생산계획일`, `계획일`, `계획일자`, `납기일`을 같은 계획일 후보로 인식한다. 단, 계산 기준은 항상 `[80]누수/규격검사` 계획일 + 5일이다.
- 생산요청 데이터의 날짜 컬럼도 `계획일자`, `계획일`, `생산계획일자`, `생산 계획일`, `납기일자`를 같은 요청 기준일 후보로 인식한다.
- `[80]누수/규격검사` 생산 계획일이 없는 행은 생산완료예상일을 `미계획`으로 표시한다.
- 생산상태는 계획일이 없으면 `미계획`, 계획일이 있고 생산부족수량이 0이면 `생산완료`, 계획일이 있고 생산부족수량이 0보다 크면 `생산중`으로 표시한다.
- 기간 필터는 생산완료예상일 기준으로 적용한다. `전체`는 미계획 행도 포함하고, 날짜 기간 필터는 계획일이 있는 행만 표시한다.
- 제품분류 필터는 제품 분류별 진도현황의 `1DAY / FRP / 기타` 구분과 같은 기준을 사용한다.
