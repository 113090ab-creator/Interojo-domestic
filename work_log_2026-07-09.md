# 작업 로그 - 2026-07-09

## 포장 CAPA 기준 추가
- 포장 현황의 `포장 우선순위` 영역에 `포장 CAPA (PCS)` 입력 기준을 추가했다.
- CAPA가 비어 있거나 0이면 기존처럼 우선순위 기준 긴급 포장 리스트를 표시한다.
- CAPA가 입력되면 우선순위 정렬 결과를 따라 포장부족 PCS를 차례로 반영하고, `CAPA반영수량(PCS)` 컬럼에 실제 반영할 수량을 표시한다.
- 마지막 행은 남은 CAPA만큼만 부분 반영할 수 있도록 정리했다.
- 엑셀 다운로드의 `긴급 포장 리스트`도 화면과 같은 CAPA 기준을 사용하도록 맞췄다.

## 표준 반영
- 포장 CAPA는 표시 단위와 동일하게 PCS 기준으로만 입력하고 계산한다.
- CAPA 미입력 상태에서는 기존 우선순위 리스트의 의미와 정렬 기준을 유지한다.
- CAPA 입력 상태에서는 고정 상위 20행 제한보다 입력 CAPA를 끝까지 소진하는 것을 우선한다.
- CAPA 로직 검증 기준은 `CAPA반영수량(PCS)` 합계가 입력 CAPA와 일치하는지 확인하는 것으로 정했다. 단, 전체 포장부족 수량이 CAPA보다 작으면 전체 포장부족 수량까지만 반영한다.

## 검증
- `python -B -m py_compile dashboard.py` 문법 검증을 통과했다.
- 실제 파일 기준으로 CAPA 100,000 PCS를 입력했을 때 `CAPA반영수량(PCS)` 합계가 100,000 PCS로 계산되는 것을 확인했다.
- 같은 검증에서 CAPA 적용 행은 2건, 마지막 행의 부분 반영 수량은 39,640 PCS로 확인했다.
