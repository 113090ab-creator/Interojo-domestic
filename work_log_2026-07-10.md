# 2026-07-10 작업 로그

## 재고이동현황 원천 반영
- `포장설비투입현황.xlsx`에 추가된 `재고이동현황` 시트 구조를 확인했다.
- 주요 컬럼: `상태`, `출고창고`, `이동일자`, `입고창고`, `품목코드`, `품명`, `Lot No.`, `이동수량`.
- 대시보드 입고 실적 원천을 다음 순서로 정리했다.
  - `재고이동현황` 시트가 있으면 우선 사용한다.
  - `상태`가 `확인` 또는 `완료`인 행만 반영한다.
  - `입고창고`가 `용마창고` 또는 `샘플창고`인 행의 `이동수량`을 입고 수량으로 사용한다.
  - `재고이동현황`이 없을 때만 기존 `용마이동현황` + `샘플이동`을 fallback으로 사용한다.

## 검증 결과
- `재고이동현황` 원본 행 수: 4,961건
- 원본 이동수량 합계: 157,799
- 제외 기준: `상태=저장` 1,233
- 대시보드 반영 행 수: 4,881건
- 대시보드 반영 입고수량 합계: 156,566
- `용마창고`: 145,998
- `샘플창고`: 11,801

## 표준 반영
- `dashboard_standards.md`에 `재고이동현황` 우선 원천 기준을 추가했다.
- 샘플 입고는 `재고이동현황`의 `샘플창고` 이동수량을 기준으로 반영하도록 표준화했다.

## 검증 명령
- `python -B -m py_compile dashboard.py`
- `normalize_packing_workbook()` 기준 입고 정규화 합계 확인
- `load_dashboard_data()` 전체 데이터 로딩 확인
